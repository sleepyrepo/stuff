
//just a file with function prototype and data declar
char* funcOne();

typedef struct Super{		//define struct with typedef, no need to struct Super when declare var
  char* str;
} Super;

struct Lame{			//normal struct define, need struct Lame lame when declare var
  char* str;
};
